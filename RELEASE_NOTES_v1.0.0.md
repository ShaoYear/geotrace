# GeoTrace v1.0.0

GeoTrace 首个正式版本。

让没有 GPS 的相机，也能拥有照片定位——用手机记录 GPS 轨迹，按拍摄时间自动把精确位置写入相机照片的 EXIF。

## 核心功能

- **GPS 轨迹记录**：前台服务持续记录，锁屏 / 后台不中断，实时显示时长、距离、定位点数、速度与精度
- **GPX 导入 / 导出**：支持标准 GPX 1.1，可与外部设备和软件互通
- **相机照片导入**：相册多选、系统照片选择器、文件夹添加，支持 JPG / PNG / HEIC，自动识别并跳过 RAW
- **自动定位匹配**：按 EXIF 拍摄时间匹配轨迹，相邻轨迹点线性插值；支持时钟偏移校准与最大匹配时间保护
- **安全写入**：默认在 `Pictures/GeoTrace/Geotagged/` 生成带 GPS 的副本，原照片不修改；覆盖模式采用安全写入流程
- **手动定位**：高德地图点选、POI 关键词搜索、逆地理编码地址
- **照片管理**：完整 EXIF 查看、拍摄时间编辑、批量改时间 / 改位置、多选全选
- **最近修改记录**：快速回看处理过的照片，拍摄时间与修改时间严格分开
- **浅色 / 深色 / 跟随系统**主题
- **完全本地**：无账号、无服务器，照片与轨迹不出设备

## 版本信息

```text
Android 8.0+
Minimum SDK: 26
Target SDK: 36
Version: 1.0.0 (versionCode 1)
```

## 安装

下载本 Release 中的 `GeoTrace-v1.0.0.apk` 后安装；首次使用请授予位置与照片权限（详见应用内流程与使用文档）。
